"use client";
import React from "react";

const AddToCart = () => {
  return (
    <button
      onClick={() => {
        console.log("clicked");
      }}
    >
      Click me
    </button>
  );
};

export default AddToCart;
